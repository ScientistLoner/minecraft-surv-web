# Веб-приложение
PORT=3000
HOST=0.0.0.0
DEV_MODE=false

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=minecraft_db
DB_USER=mc_admin
DB_PASSWORD=change_me
# Для docker-compose (postgres + app) используется POSTGRES_PASSWORD:
POSTGRES_PASSWORD=change_me

# JWT (обязательно замените на свой)
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# Minecraft-сервер (для статуса и топа)
MC_HOST=localhost
MC_PORT=25565

# Telegram для связи (показывается на странице)
CONTACT_TELEGRAM=@Scientistloner